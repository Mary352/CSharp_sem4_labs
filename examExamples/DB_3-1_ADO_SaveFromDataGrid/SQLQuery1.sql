use master;
create database ADO_SaveFromDataGrid;

use ADO_SaveFromDataGrid;

create table students
(
	Surname varchar(30),
	[Name] varchar(30)
);

--delete

--no
--drop database ADO_SaveFromDataGrid;
